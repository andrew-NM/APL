# pypy is another version of python that focuses mainly on speed and performance
# it uses a just in time compiler that turns parts of the code into machine code while the program is running which makes many programs run faster than on cpython

# jython is a version of python that runs on the java virtual machine
# it lets you use java libraries and tools directly from python which makes it helpful for working with java systems